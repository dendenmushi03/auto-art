{
  "name": "auto-art",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "install:python": "python3 -m pip install -r requirements.txt || python -m pip install -r requirements.txt",
    "postinstall": "npm run install:python",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "mongoose": "^7.0.0",
    "stripe": "^14.0.0"
  }
}
